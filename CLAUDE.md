@AGENTS.md

## Claude Code specific

`../mttbar/mtt/` (the reference analysis, see AGENTS.md's Layout table) is
pre-approved for Read/Grep/Glob in `.claude/settings.local.json`, so explore it
freely there without asking for permission first.
